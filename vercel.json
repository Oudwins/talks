{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/talks/:deck/:path*", "destination": "/talks/:deck/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
